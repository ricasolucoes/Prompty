// Placeholder — substitua rodando: pnpm gen:types
// Requer: supabase link --project-ref ouoxxwbiqgecaysoybpv
export type Database = Record<string, unknown>
export type Tables<T extends string> = Record<T, unknown>
