---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: [01-01]
files_modified:
  - supabase/config.toml
  - supabase/migrations/001_schema.sql
  - supabase/migrations/002_rls.sql
  - supabase/migrations/003_triggers.sql
  - supabase/migrations/004_views.sql
  - src/types/database.types.ts
  - .env.example
  - package.json
autonomous: false
requirements: [INFR-01, INFR-02, INFR-04, GAME-01, GAME-02, GAME-05]
must_haves:
  truths:
    - "All 10 tables exist with correct columns, constraints, and indexes"
    - "Every table has RLS enabled with explicit policies for SELECT, INSERT, UPDATE, DELETE per relevant role"
    - "point_events rejects all client INSERT/UPDATE/DELETE via WITH CHECK (false) policies"
    - "AFTER INSERT triggers on prompty_likes and prompty_saves write to point_events with ON CONFLICT DO NOTHING (idempotent)"
    - "AFTER DELETE triggers on prompty_likes and prompty_saves insert reversal rows (negative points)"
    - "profiles.points and profiles.level are materialized columns updated by triggers"
    - "Storage bucket prompty-images enforces fileSizeLimit 2097152 and webp/jpeg/png MIME types"
    - "Generated TypeScript Database type covers all tables"
  artifacts:
    - path: "supabase/migrations/001_schema.sql"
      provides: "10 tables + UNIQUE constraints + indexes"
      contains: "CREATE TABLE point_events"
    - path: "supabase/migrations/002_rls.sql"
      provides: "Explicit policies per table per role"
      contains: "WITH CHECK (false)"
    - path: "supabase/migrations/003_triggers.sql"
      provides: "Idempotent point-awarding triggers"
      contains: "SECURITY DEFINER"
    - path: "supabase/migrations/004_views.sql"
      provides: "feed_hot view scaffold"
      contains: "feed_hot"
    - path: "src/types/database.types.ts"
      provides: "TS types generated from schema"
      contains: "export type Database"
  key_links:
    - from: "prompty_likes (INSERT)"
      to: "point_events"
      via: "AFTER INSERT trigger handle_like_received"
      pattern: "AFTER INSERT ON prompty_likes"
    - from: "prompty_likes (DELETE)"
      to: "point_events"
      via: "AFTER DELETE trigger handle_like_reversed"
      pattern: "AFTER DELETE ON prompty_likes"
    - from: "point_events"
      to: "profiles.points/level"
      via: "trigger UPDATE on profiles"
      pattern: "UPDATE profiles SET\\s+points"
---

<objective>
Build the entire Supabase data foundation: 10 tables in `001_schema.sql`, RLS policies on every table in `002_rls.sql`, idempotent point-awarding triggers in `003_triggers.sql`, the feed_hot view scaffold in `004_views.sql`, the `prompty-images` Storage bucket configured with size + MIME limits, and generated TypeScript types from the live schema.

Purpose: This plan implements the data + security spine that EVERY subsequent phase depends on. RLS gaps or trigger bugs introduced here propagate forever.
Output: 4 migration files, supabase/config.toml with bucket config, src/types/database.types.ts, validated by SQL role tests + trigger idempotency tests from plan 01-01.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@CLAUDE.md
@supabase/tests/rls_roles.sql
@supabase/tests/triggers_idempotency.sql
</context>

<user_setup>
- service: supabase
  why: "Provision the project that hosts schema, RLS, triggers, Storage"
  env_vars:
    - name: NEXT_PUBLIC_SUPABASE_URL
      source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
    - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
      source: "Supabase Dashboard -> Project Settings -> API -> Project API keys -> anon public"
    - name: SUPABASE_DB_URL
      source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> URI (use the pgbouncer pooled URL)"
  dashboard_config:
    - task: "Configure Site URL and Redirect URLs"
      location: "Supabase Dashboard -> Authentication -> URL Configuration"
      details: "Site URL = http://localhost:3000; Redirect URLs += http://localhost:3000/auth/callback"
    - task: "Enable email confirmations"
      location: "Supabase Dashboard -> Authentication -> Providers -> Email"
      details: "Confirm email = ON"
</user_setup>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Supabase project + write 001_schema.sql (10 tables)</name>
  <files>supabase/config.toml, supabase/migrations/001_schema.sql, .env.example, package.json</files>
  <read_first>
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/phases/01-foundation/01-RESEARCH.md (Code Examples 001_schema.sql section)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/phases/01-foundation/01-CONTEXT.md (Gamification Infrastructure — point event types and values)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/PROJECT.md (database schema reference)
  </read_first>
  <action>
    Install Supabase CLI as dev dep: `npm install -D supabase`. Add `package.json` scripts:
    ```json
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:push": "supabase db push",
    "db:types": "supabase gen types typescript --local > src/types/database.types.ts",
    "db:test:rls": "psql \"$SUPABASE_DB_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/rls_roles.sql",
    "db:test:triggers": "psql \"$SUPABASE_DB_URL\" -v ON_ERROR_STOP=1 -f supabase/tests/triggers_idempotency.sql"
    ```

    Run `npx supabase init` (creates `supabase/config.toml` and folder structure). In `supabase/config.toml` set:
    ```toml
    [storage.buckets.prompty-images]
    public = true
    file_size_limit = "2MiB"
    allowed_mime_types = ["image/jpeg", "image/png", "image/webp"]
    ```

    Create `.env.example` with:
    ```
    NEXT_PUBLIC_SUPABASE_URL=
    NEXT_PUBLIC_SUPABASE_ANON_KEY=
    SUPABASE_DB_URL=
    ```

    Create `supabase/migrations/001_schema.sql` with all 10 tables. Required content:

    1. `profiles` — `id uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE`, `username text UNIQUE` with CHECK `username ~ '^[a-zA-Z0-9_-]{3,30}$'`, `display_name text`, `avatar_url text`, `bio text`, `points integer NOT NULL DEFAULT 0`, `level integer NOT NULL DEFAULT 1`, `is_admin boolean NOT NULL DEFAULT false`, `created_at`, `updated_at` timestamptz DEFAULT now().

    2. `promptys` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `author_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `title text NOT NULL`, `description text`, `template text`, `negative_prompt text`, `inputs_schema jsonb NOT NULL DEFAULT '[]'::jsonb`, `model_targets text[] NOT NULL DEFAULT '{}'`, `difficulty text NOT NULL DEFAULT 'beginner' CHECK (difficulty IN ('beginner','intermediate','advanced'))`, `style_tags text[] NOT NULL DEFAULT '{}'`, `cover_image_url text`, `status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft','published','unlisted','flagged','hidden','removed'))`, `parent_id uuid REFERENCES promptys(id)`, `created_at`, `updated_at` timestamptz DEFAULT now().

    3. `prompty_versions` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `prompty_id uuid NOT NULL REFERENCES promptys(id) ON DELETE CASCADE`, `version_number integer NOT NULL`, `snapshot jsonb NOT NULL`, `created_at timestamptz DEFAULT now()`, UNIQUE(prompty_id, version_number).

    4. `prompty_tests` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `prompty_id uuid NOT NULL REFERENCES promptys(id) ON DELETE CASCADE`, `user_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `inputs jsonb NOT NULL`, `result_image_url text NOT NULL`, `notes text`, `rating integer CHECK (rating BETWEEN 1 AND 5)`, `created_at timestamptz DEFAULT now()`.

    5. `prompty_ratings` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `prompty_id uuid NOT NULL REFERENCES promptys(id) ON DELETE CASCADE`, `user_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `rating integer NOT NULL CHECK (rating BETWEEN 1 AND 5)`, `created_at timestamptz DEFAULT now()`, UNIQUE(prompty_id, user_id).

    6. `prompty_likes` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `prompty_id uuid NOT NULL REFERENCES promptys(id) ON DELETE CASCADE`, `user_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `created_at timestamptz DEFAULT now()`, UNIQUE(prompty_id, user_id).

    7. `prompty_saves` — same shape as prompty_likes (likes/saves are distinct tables).

    8. `prompty_comments` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `prompty_id uuid NOT NULL REFERENCES promptys(id) ON DELETE CASCADE`, `user_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `body text NOT NULL`, `is_helpful boolean NOT NULL DEFAULT false`, `created_at timestamptz DEFAULT now()`.

    9. `prompty_reports` — `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `target_type text NOT NULL CHECK (target_type IN ('prompty','comment'))`, `target_id uuid NOT NULL`, `reporter_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `reason text NOT NULL`, `status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','resolved','dismissed'))`, `created_at timestamptz DEFAULT now()`.

    10. `point_events` — `id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY`, `user_id uuid NOT NULL REFERENCES profiles(id) ON DELETE CASCADE`, `event_type text NOT NULL`, `reference_id uuid` (nullable for non-FK events), `points integer NOT NULL`, `created_at timestamptz NOT NULL DEFAULT now()`, `CONSTRAINT point_events_idempotent UNIQUE (user_id, event_type, reference_id)`.

    Create indexes: `idx_profiles_username` on profiles(username), `idx_point_events_user` on point_events(user_id), `idx_promptys_author_status` on promptys(author_id, status), `idx_promptys_status_created` on promptys(status, created_at DESC), `idx_prompty_likes_prompty` on prompty_likes(prompty_id), `idx_prompty_likes_user` on prompty_likes(user_id).

    Enable `pgcrypto` extension at top of file: `CREATE EXTENSION IF NOT EXISTS "pgcrypto";`.
  </action>
  <verify>
    <automated>npx supabase start && npx supabase db reset --local && psql "postgresql://postgres:postgres@localhost:54322/postgres" -c "SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('profiles','promptys','prompty_versions','prompty_tests','prompty_ratings','prompty_likes','prompty_saves','prompty_comments','prompty_reports','point_events')" -t -A | grep -q "^10$"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/001_schema.sql` contains `CREATE TABLE profiles`
    - File contains all 10 table names: profiles, promptys, prompty_versions, prompty_tests, prompty_ratings, prompty_likes, prompty_saves, prompty_comments, prompty_reports, point_events
    - File contains `CONSTRAINT point_events_idempotent UNIQUE (user_id, event_type, reference_id)`
    - File contains `CHECK (username ~ '^[a-zA-Z0-9_-]{3,30}$')`
    - File contains `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`
    - `supabase/config.toml` contains `[storage.buckets.prompty-images]` and `file_size_limit = "2MiB"`
    - `.env.example` contains `NEXT_PUBLIC_SUPABASE_URL=` and `NEXT_PUBLIC_SUPABASE_ANON_KEY=`
    - After `supabase db reset --local`, query `SELECT count(*) FROM information_schema.tables WHERE table_schema='public'` returns >= 10
  </acceptance_criteria>
  <done>Local Supabase running, schema applied, all 10 tables present, Storage bucket configured.</done>
</task>

<task type="auto">
  <name>Task 2: Write 002_rls.sql with explicit policies on all 10 tables</name>
  <files>supabase/migrations/002_rls.sql</files>
  <read_first>
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: RLS Policy Structure; Pitfall 1: RLS Enabled but Incomplete Policies)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/supabase/migrations/001_schema.sql (the schema file just created)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/supabase/tests/rls_roles.sql (the validation script this must satisfy)
  </read_first>
  <action>
    Create `supabase/migrations/002_rls.sql`. For EVERY table from 001_schema.sql:
    - `ALTER TABLE {table} ENABLE ROW LEVEL SECURITY;`
    - Add explicit policies per the matrix below. Use `TO authenticated` and/or `TO anon` explicitly (never omit role).

    Policy matrix (one CREATE POLICY statement per cell):

    `profiles`:
    - `profiles_select_public` SELECT TO anon, authenticated USING (true)
    - `profiles_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = id)
    - `profiles_update_own` UPDATE TO authenticated USING (auth.uid() = id) WITH CHECK (auth.uid() = id)
    - (no DELETE policy — cascades from auth.users)

    `promptys`:
    - `promptys_select_public` SELECT TO anon, authenticated USING (status IN ('published','unlisted'))
    - `promptys_select_own` SELECT TO authenticated USING (auth.uid() = author_id)
    - `promptys_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = author_id)
    - `promptys_update_own` UPDATE TO authenticated USING (auth.uid() = author_id) WITH CHECK (auth.uid() = author_id)
    - `promptys_delete_own` DELETE TO authenticated USING (auth.uid() = author_id)

    `prompty_versions`:
    - `versions_select_visible` SELECT TO anon, authenticated USING (EXISTS (SELECT 1 FROM promptys p WHERE p.id = prompty_id AND (p.status IN ('published','unlisted') OR p.author_id = auth.uid())))
    - `versions_insert_own` INSERT TO authenticated WITH CHECK (EXISTS (SELECT 1 FROM promptys p WHERE p.id = prompty_id AND p.author_id = auth.uid()))

    `prompty_tests`:
    - `tests_select_public` SELECT TO anon, authenticated USING (true)
    - `tests_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = user_id)
    - `tests_update_own` UPDATE TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)
    - `tests_delete_own` DELETE TO authenticated USING (auth.uid() = user_id)

    `prompty_ratings`:
    - `ratings_select_public` SELECT TO anon, authenticated USING (true)
    - `ratings_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = user_id)
    - `ratings_update_own` UPDATE TO authenticated USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id)
    - `ratings_delete_own` DELETE TO authenticated USING (auth.uid() = user_id)

    `prompty_likes` (same shape as ratings — public select, own insert/delete; no update).

    `prompty_saves`:
    - `saves_select_own` SELECT TO authenticated USING (auth.uid() = user_id)
    - `saves_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = user_id)
    - `saves_delete_own` DELETE TO authenticated USING (auth.uid() = user_id)

    `prompty_comments`:
    - `comments_select_public` SELECT TO anon, authenticated USING (true)
    - `comments_insert_own` INSERT TO authenticated WITH CHECK (auth.uid() = user_id)
    - `comments_delete_own` DELETE TO authenticated USING (auth.uid() = user_id)

    `prompty_reports`:
    - `reports_select_admin` SELECT TO authenticated USING ((SELECT is_admin FROM profiles WHERE id = auth.uid()))
    - `reports_insert_authenticated` INSERT TO authenticated WITH CHECK (auth.uid() = reporter_id)

    `point_events` (THE CRITICAL ONE — INFR-02 / GAME-02):
    - `point_events_select_own` SELECT TO authenticated USING (auth.uid() = user_id)
    - `point_events_no_insert` INSERT TO authenticated, anon WITH CHECK (false)
    - `point_events_no_update` UPDATE TO authenticated, anon USING (false)
    - `point_events_no_delete` DELETE TO authenticated, anon USING (false)

    Storage policies for `prompty-images` bucket (append at end of file):
    - `prompty_images_select_public` ON storage.objects FOR SELECT TO anon, authenticated USING (bucket_id = 'prompty-images')
    - `prompty_images_insert_own_folder` ON storage.objects FOR INSERT TO authenticated WITH CHECK (bucket_id = 'prompty-images' AND (storage.foldername(name))[1] = auth.uid()::text)
    - `prompty_images_update_own_folder` ON storage.objects FOR UPDATE TO authenticated USING (bucket_id = 'prompty-images' AND (storage.foldername(name))[1] = auth.uid()::text)
    - `prompty_images_delete_own_folder` ON storage.objects FOR DELETE TO authenticated USING (bucket_id = 'prompty-images' AND (storage.foldername(name))[1] = auth.uid()::text)
  </action>
  <verify>
    <automated>npx supabase db reset --local && npm run db:test:rls</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/002_rls.sql` contains exactly 10 occurrences of `ENABLE ROW LEVEL SECURITY` (one per table)
    - File contains `WITH CHECK (false)` (point_events INSERT block)
    - File contains `point_events_no_insert`, `point_events_no_update`, `point_events_no_delete`
    - File contains `(storage.foldername(name))[1] = auth.uid()::text`
    - `npm run db:test:rls` exits 0 against the local DB
  </acceptance_criteria>
  <done>RLS enabled and policies present on all 10 tables; rls_roles.sql script passes.</done>
</task>

<task type="auto">
  <name>Task 3: Write 003_triggers.sql + 004_views.sql, then generate types</name>
  <files>supabase/migrations/003_triggers.sql, supabase/migrations/004_views.sql, src/types/database.types.ts</files>
  <read_first>
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Idempotent Points Trigger; Pitfall 2: SECURITY DEFINER; Pitfall 5: double-fire)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/.planning/phases/01-foundation/01-CONTEXT.md (Gamification Infrastructure — exact event_types and points values)
    - /Users/sierra/Dev/RicaSolucoes/Prompty/supabase/tests/triggers_idempotency.sql (validation script this must satisfy)
  </read_first>
  <action>
    Create `supabase/migrations/003_triggers.sql`. Implement:

    **A. Helper function `compute_level(p integer) RETURNS integer LANGUAGE sql IMMUTABLE AS $$ ... $$;`** — return 7 if p >= 25000, 6 if >= 10000, 5 if >= 5000, 4 if >= 1500, 3 if >= 500, 2 if >= 100, else 1. Use a CASE expression.

    **B. Function `update_profile_points(target_user uuid)` LANGUAGE plpgsql SECURITY DEFINER**: recomputes `points = COALESCE(SUM(points),0) FROM point_events WHERE user_id = target_user`, then sets `level = compute_level(points)`. Single UPDATE statement on profiles.

    **C. Trigger function `handle_prompty_published()` SECURITY DEFINER**: AFTER UPDATE OF status ON promptys, when NEW.status='published' AND (OLD.status IS DISTINCT FROM 'published'), INSERT into point_events (NEW.author_id, 'prompty_published', NEW.id, 50) ON CONFLICT DO NOTHING; CALL update_profile_points(NEW.author_id). Also fire on AFTER INSERT when NEW.status='published'.

    **D. Trigger function `handle_test_submitted()` SECURITY DEFINER**: AFTER INSERT ON prompty_tests, INSERT (NEW.user_id, 'test_submitted', NEW.id, 15) ON CONFLICT DO NOTHING; update_profile_points(NEW.user_id).

    **E. Trigger function `handle_like_received()` SECURITY DEFINER**: AFTER INSERT ON prompty_likes — looks up author_id from promptys, INSERT (author_id, 'like_received', NEW.id, 3) ON CONFLICT DO NOTHING; update_profile_points(author_id).

    **F. Trigger function `handle_like_reversed()` SECURITY DEFINER**: AFTER DELETE ON prompty_likes — looks up author_id, INSERT (author_id, 'like_reversed', OLD.id, -3) ON CONFLICT DO NOTHING; update_profile_points(author_id).

    **G. Trigger function `handle_save_reversed()` SECURITY DEFINER**: AFTER DELETE ON prompty_saves — INSERT (NEW.user_id, 'save_reversed', OLD.id, -1) ON CONFLICT DO NOTHING; update_profile_points(OLD.user_id). (No save_received event in Phase 1.)

    **H. Trigger function `handle_rating_given()` SECURITY DEFINER**: AFTER INSERT ON prompty_ratings — INSERT (NEW.user_id, 'rating_given', NEW.id, 10) ON CONFLICT DO NOTHING; update_profile_points(NEW.user_id).

    **I. Trigger function `handle_helpful_comment()` SECURITY DEFINER**: AFTER UPDATE OF is_helpful ON prompty_comments, when NEW.is_helpful=true AND OLD.is_helpful=false: INSERT (NEW.user_id, 'helpful_comment', NEW.id, 10) ON CONFLICT DO NOTHING; update_profile_points(NEW.user_id).

    Attach each function with explicit `CREATE TRIGGER on_<name> AFTER {INSERT|DELETE|UPDATE} ON <table> FOR EACH ROW EXECUTE FUNCTION <fn>();`.

    Also: **on auth.users INSERT** create a profile row. Function `handle_new_user()` SECURITY DEFINER: INSERT INTO profiles (id) VALUES (NEW.id) ON CONFLICT DO NOTHING. Trigger AFTER INSERT ON auth.users.

    Create `supabase/migrations/004_views.sql` with a placeholder feed_hot view (Phase 3 will extend):
    ```sql
    -- feed_hot view scaffold — Phase 3 will extend with hot-score algorithm
    CREATE OR REPLACE VIEW feed_hot AS
    SELECT
      p.id,
      p.author_id,
      p.title,
      p.cover_image_url,
      p.status,
      p.created_at,
      COALESCE((SELECT count(*) FROM prompty_likes pl WHERE pl.prompty_id = p.id), 0) AS like_count,
      COALESCE((SELECT count(*) FROM prompty_tests pt WHERE pt.prompty_id = p.id), 0) AS test_count,
      COALESCE((SELECT avg(rating) FROM prompty_ratings pr WHERE pr.prompty_id = p.id), 0) AS avg_rating
    FROM promptys p
    WHERE p.status = 'published';
    ```

    After migrations apply, generate types:
    `npx supabase gen types typescript --local > src/types/database.types.ts`
  </action>
  <verify>
    <automated>npx supabase db reset --local && npm run db:test:triggers && grep -c "SECURITY DEFINER" supabase/migrations/003_triggers.sql && grep -q "export type Database" src/types/database.types.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/003_triggers.sql` contains `SECURITY DEFINER` at least 8 times (helper + 7 trigger functions + new-user)
    - File contains `ON CONFLICT DO NOTHING` at least 7 times
    - File contains all event_type strings: `prompty_published`, `test_submitted`, `like_received`, `like_reversed`, `save_reversed`, `rating_given`, `helpful_comment`
    - File contains `compute_level` and `update_profile_points`
    - File contains `AFTER INSERT ON auth.users`
    - File `supabase/migrations/004_views.sql` contains `CREATE OR REPLACE VIEW feed_hot`
    - File `src/types/database.types.ts` exists and contains `export type Database`
    - `npm run db:test:triggers` exits 0
  </acceptance_criteria>
  <done>All triggers active, idempotent, and validated by triggers_idempotency.sql; TypeScript types generated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Supabase project URL configuration</name>
  <files>N/A (human-only verification of dashboard configuration)</files>
  <action>Human operator must complete Supabase Dashboard setup as described in the &lt;how-to-verify&gt; block: create the Cloud project, copy keys to .env.local, configure Site URL and Redirect URLs, enable email confirmations, and run the SQL test scripts locally. No file changes performed by Claude.</action>
  <verify><automated>echo "manual checkpoint — verify human approval"</automated></verify>
  <done>Operator types &quot;approved&quot; after dashboard config and SQL tests pass.</done>
  <what-built>Local Supabase stack with full schema, RLS, triggers, views, and Storage bucket. TypeScript types generated.</what-built>
  <how-to-verify>
    1. Confirm a Supabase Cloud project exists (or create one at https://supabase.com/dashboard).
    2. Copy Project URL + anon key into `.env.local` (do not commit).
    3. In Supabase Dashboard -> Authentication -> URL Configuration, confirm Site URL = `http://localhost:3000` and Redirect URLs include `http://localhost:3000/auth/callback`.
    4. In Authentication -> Providers -> Email, confirm "Confirm email" is ON.
    5. Run `npm run db:test:rls` and `npm run db:test:triggers` against the LOCAL DB — both must exit 0.
    6. Reply with "approved" or describe what failed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- All 10 tables exist; `\dt public.*` lists them
- `npm run db:test:rls` passes (INFR-01, INFR-02, GAME-02)
- `npm run db:test:triggers` passes (GAME-01, GAME-05)
- `src/types/database.types.ts` exports Database type
- Supabase Storage bucket `prompty-images` exists with size + MIME limits (INFR-04)
</verification>

<success_criteria>
Database foundation locked: schema, security, triggers, views, types. INFR-01, INFR-02, INFR-04, GAME-01, GAME-02, GAME-05 satisfied. Plans 04 and 05 can build on these types.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
