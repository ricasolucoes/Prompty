---
phase: 02-l2-curador-descoberta
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/layout/TabBar.tsx
  - src/components/layout/TabBar.test.tsx
  - src/components/ui/Icon.tsx
  - src/App.tsx
  - src/lib/constants/categories.ts
autonomous: true
requirements:
  - CUR-03
  - FEED-06
  - FEED-07

must_haves:
  truths:
    - "TabBar TABS array contains a Salvos entry between Feed and Buscar with minLevel='L2'"
    - "L2 user sees exactly 4 tabs in this order: Feed, Salvos, Buscar, Perfil"
    - "L1 user still sees exactly 2 tabs (Feed + Perfil); locked tabs are absent from DOM (LEVL-07)"
    - "Icon.tsx exports IconName members: moreHorizontal, flag, tag"
    - "App.tsx registers routes /saved (SavedPage placeholder) and /search (SearchPage placeholder) inside ChromeShell"
    - "src/lib/constants/categories.ts exports CATEGORIES and MODELS arrays used by FilterChipBar"
  artifacts:
    - path: "src/components/layout/TabBar.tsx"
      provides: "Updated TABS array with Salvos minLevel=L2"
      contains: "to: '/saved'"
    - path: "src/components/layout/TabBar.test.tsx"
      provides: "Updated assertions: L1=2 tabs, L2=4 tabs, L3=6 tabs"
      contains: "toHaveLength(4)"
    - path: "src/components/ui/Icon.tsx"
      provides: "Icon paths for moreHorizontal, flag, tag"
      contains: "moreHorizontal"
    - path: "src/App.tsx"
      provides: "Routes for /saved and /search registered inside ChromeShell"
      contains: "path=\"/saved\""
    - path: "src/lib/constants/categories.ts"
      provides: "CATEGORIES and MODELS static lists for filter chips"
      exports: ["CATEGORIES", "MODELS"]
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/SavedPage.tsx"
      via: "<Route path='/saved' element={<SavedPage />} />"
      pattern: "path=\"/saved\""
    - from: "src/App.tsx"
      to: "src/pages/SearchPage.tsx"
      via: "<Route path='/search' element={<SearchPage />} />"
      pattern: "path=\"/search\""
    - from: "src/components/layout/TabBar.tsx"
      to: "/saved route"
      via: "TABS array entry with to:'/saved'"
      pattern: "to: '/saved'"
---

<objective>
Wire the navigation chrome for Phase 2: add the Salvos tab to TabBar (between Feed and Buscar), register the /saved and /search routes in App.tsx, add three new icons (moreHorizontal, flag, tag), and create the static categories/models constants module that FilterChipBar will consume.

Purpose: TabBar + routes are the L2 entry points. Adding them in their own plan keeps the change atomic (TabBar test must be updated in the same commit per RESEARCH.md Pitfall 1) and unblocks parallel plans 02-03 (Search), 02-04 (Saved), and 02-05 (Sheets/Reports) which can write their pages knowing the routes exist. Placeholder pages render a TODO message until their owning plan replaces them.

Output: Updated TabBar (4 L2 tabs), updated TabBar test asserting 4 L2 tabs, 3 new icon paths, two new routes, and one new constants module.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-l2-curador-descoberta/02-CONTEXT.md
@.planning/phases/02-l2-curador-descoberta/02-RESEARCH.md
@.planning/phases/02-l2-curador-descoberta/02-UI-SPEC.md
@.planning/phases/02-l2-curador-descoberta/02-01-PLAN.md
@src/components/layout/TabBar.tsx
@src/components/layout/TabBar.test.tsx
@src/components/ui/Icon.tsx
@src/App.tsx
@CLAUDE.md

<interfaces>
<!-- Existing TabBar definition this plan extends -->

From src/components/layout/TabBar.tsx:
```typescript
interface TabDef {
  to: string
  icon: IconName
  label: string
  minLevel: 'L1' | 'L2' | 'L3'
}

const TABS: TabDef[] = [
  { to: '/',        icon: 'home',    label: 'Feed',    minLevel: 'L1' },
  { to: '/search',  icon: 'search',  label: 'Buscar',  minLevel: 'L2' },
  { to: '/create',  icon: 'sparkle', label: 'Criar',   minLevel: 'L3' },
  { to: '/ranking', icon: 'flame',   label: 'Ranking', minLevel: 'L3' },
  { to: '/profile', icon: 'user',    label: 'Perfil',  minLevel: 'L1' },
]
```

From src/components/ui/Icon.tsx:
```typescript
export type IconName =
  | 'home' | 'user' | 'heart' | 'heartFill' | 'copy' | 'check'
  | 'star' | 'starFill' | 'chevronL' | 'x' | 'sparkle' | 'wand'
  | 'gemini' | 'image' | 'flame' | 'search' | 'bookmark' | 'lock'
```

From src/App.tsx ChromeShell:
```typescript
<Routes>
  <Route path="/" element={<FeedPage />} />
  <Route path="/profile" element={<ProfilePage />} />
  <Route path="/u/:username" element={<PublicProfilePage />} />
  <Route path="/p/:slug" element={<PromptyDetailPage />} />
  <Route path="*" element={<Navigate to="/" replace />} />
</Routes>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Salvos tab to TabBar + update TabBar test for 4 L2 tabs</name>
  <files>src/components/layout/TabBar.tsx, src/components/layout/TabBar.test.tsx</files>
  <read_first>
    - src/components/layout/TabBar.tsx (current TABS array — insert Salvos between Feed and Buscar)
    - src/components/layout/TabBar.test.tsx (current test — line 55 currently asserts toHaveLength(3) for L2, must become toHaveLength(4))
    - .planning/phases/02-l2-curador-descoberta/02-UI-SPEC.md (TabBar L2 — 4 tabs section)
    - .planning/phases/02-l2-curador-descoberta/02-RESEARCH.md (Pitfall 1 — TabBar test breaks)
  </read_first>
  <action>
    Edit `src/components/layout/TabBar.tsx`. Replace the TABS array with this exact contents (Salvos inserted between Feed and Buscar):

    ```typescript
    const TABS: TabDef[] = [
      { to: '/',        icon: 'home',     label: 'Feed',    minLevel: 'L1' },
      { to: '/saved',   icon: 'bookmark', label: 'Salvos',  minLevel: 'L2' },
      { to: '/search',  icon: 'search',   label: 'Buscar',  minLevel: 'L2' },
      { to: '/create',  icon: 'sparkle',  label: 'Criar',   minLevel: 'L3' },
      { to: '/ranking', icon: 'flame',    label: 'Ranking', minLevel: 'L3' },
      { to: '/profile', icon: 'user',     label: 'Perfil',  minLevel: 'L1' },
    ]
    ```

    DO NOT modify the LEVEL_ORDER constant, levelGteMin helper, or render logic — only the TABS array changes.

    Edit `src/components/layout/TabBar.test.tsx`. Replace the L2 test and L3 test with these exact contents:

    ```typescript
    it('renders 4 tabs for L2 (Feed + Salvos + Buscar + Perfil)', () => {
      setLevel('L2', 100)
      renderTabBar()
      expect(screen.getAllByRole('link')).toHaveLength(4)
      expect(screen.getByLabelText('Feed')).toBeInTheDocument()
      expect(screen.getByLabelText('Salvos')).toBeInTheDocument()
      expect(screen.getByLabelText('Buscar')).toBeInTheDocument()
      expect(screen.getByLabelText('Perfil')).toBeInTheDocument()
      expect(screen.queryByLabelText('Criar')).not.toBeInTheDocument()
      expect(screen.queryByLabelText('Ranking')).not.toBeInTheDocument()
    })

    it('renders 6 tabs for L3 (adds Criar + Ranking)', () => {
      setLevel('L3', 300)
      renderTabBar()
      expect(screen.getAllByRole('link')).toHaveLength(6)
      expect(screen.getByLabelText('Criar')).toBeInTheDocument()
      expect(screen.getByLabelText('Ranking')).toBeInTheDocument()
    })
    ```

    Also add a new test for the L1 absence of Salvos (LEVL-07 — locked tabs absent from DOM):

    ```typescript
    it('L1 user does NOT see Salvos tab (LEVL-07)', () => {
      setLevel('L1', 49)
      renderTabBar()
      expect(screen.queryByLabelText('Salvos')).not.toBeInTheDocument()
      expect(screen.queryByText(/salvos/i)).not.toBeInTheDocument()
    })
    ```

    Place the new L1 test inside the existing `describe('TabBar — progressive disclosure (LEVL-07)', () => { ... })` block, immediately before the existing `'renders 4 tabs for L2'` test.
  </action>
  <verify>
    <automated>grep -q "to: '/saved'" src/components/layout/TabBar.tsx && grep -q "label: 'Salvos'" src/components/layout/TabBar.tsx && grep -q "minLevel: 'L2'" src/components/layout/TabBar.tsx && grep -q "toHaveLength(4)" src/components/layout/TabBar.test.tsx && grep -q "toHaveLength(6)" src/components/layout/TabBar.test.tsx && pnpm test:run -- src/components/layout/TabBar.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/TabBar.tsx` TABS array contains exactly 6 entries
    - Salvos entry has `to: '/saved'`, `icon: 'bookmark'`, `label: 'Salvos'`, `minLevel: 'L2'` and appears between Feed (index 0) and Buscar (index 2)
    - `src/components/layout/TabBar.test.tsx` contains `toHaveLength(4)` in the L2 test (replacing the previous `toHaveLength(3)`)
    - `src/components/layout/TabBar.test.tsx` contains `toHaveLength(6)` in the L3 test (replacing the previous `toHaveLength(5)`)
    - New test `L1 user does NOT see Salvos tab (LEVL-07)` exists and passes
    - All TabBar tests pass: `pnpm test:run -- src/components/layout/TabBar.test.tsx`
  </acceptance_criteria>
  <done>TabBar shows 4 tabs for L2 (Feed + Salvos + Buscar + Perfil); test assertions updated; LEVL-07 enforced for L1.</done>
</task>

<task type="auto">
  <name>Task 2: Add new icons (moreHorizontal, flag, tag) to Icon.tsx</name>
  <files>src/components/ui/Icon.tsx</files>
  <read_first>
    - src/components/ui/Icon.tsx (existing IconName union + PATHS record — strokeWidth 1.8, viewBox 24×24)
    - .planning/phases/02-l2-curador-descoberta/02-UI-SPEC.md (New Icon Paths section with exact SVG snippets)
  </read_first>
  <action>
    Edit `src/components/ui/Icon.tsx`.

    1. Extend the `IconName` union to include three new members. The new union must read EXACTLY:

    ```typescript
    export type IconName =
      | 'home' | 'user' | 'heart' | 'heartFill' | 'copy' | 'check'
      | 'star' | 'starFill' | 'chevronL' | 'x' | 'sparkle' | 'wand'
      | 'gemini' | 'image' | 'flame' | 'search' | 'bookmark' | 'lock'
      | 'moreHorizontal' | 'flag' | 'tag'
    ```

    2. Inside the `PATHS` Record literal, after the existing `lock` entry, add three new entries (preserving alphabetic-ish grouping is NOT required — just keep them at the end):

    ```typescript
    moreHorizontal: (
      <>
        <circle cx="5"  cy="12" r="1.2" fill="currentColor" stroke="none" />
        <circle cx="12" cy="12" r="1.2" fill="currentColor" stroke="none" />
        <circle cx="19" cy="12" r="1.2" fill="currentColor" stroke="none" />
      </>
    ),
    flag: <path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1zM4 22v-7" />,
    tag: (
      <>
        <path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z" />
        <line x1="7" y1="7" x2="7.01" y2="7" />
      </>
    ),
    ```

    DO NOT modify the existing icon paths, the `Icon` component function, the `IconProps` interface, or the import statements.
  </action>
  <verify>
    <automated>grep -q "'moreHorizontal' | 'flag' | 'tag'" src/components/ui/Icon.tsx && grep -q "moreHorizontal:" src/components/ui/Icon.tsx && grep -q "flag:" src/components/ui/Icon.tsx && grep -q "tag:" src/components/ui/Icon.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/Icon.tsx` `IconName` union contains literal members `'moreHorizontal'`, `'flag'`, `'tag'`
    - `PATHS` record contains three new keys: `moreHorizontal`, `flag`, `tag`
    - `moreHorizontal` uses three `<circle>` elements with `fill="currentColor" stroke="none"` (filled dots — strokeWidth on 1.2px circles is invisible per UI-SPEC engineering note)
    - `flag` uses `<path d="M4 15s1-1 4-1 5 2 8 2 4-1 4-1V3s-1 1-4 1-5-2-8-2-4 1-4 1zM4 22v-7" />`
    - `tag` uses a path + line combo
    - `pnpm type-check` exits 0
  </acceptance_criteria>
  <done>Three new icons available via `<Icon name="moreHorizontal" />`, `<Icon name="flag" />`, `<Icon name="tag" />` — consumed by OptionsSheet and CommunityResults in subsequent plans.</done>
</task>

<task type="auto">
  <name>Task 3: Register /saved + /search routes in App.tsx with placeholder pages + create categories constants</name>
  <files>src/App.tsx, src/lib/constants/categories.ts</files>
  <read_first>
    - src/App.tsx (current ChromeShell route block)
    - .planning/phases/02-l2-curador-descoberta/02-UI-SPEC.md (Category chips static list + Model chips static list)
    - src/pages/FeedPage.tsx (route consumer pattern for reference)
  </read_first>
  <action>
    1. Create `src/lib/constants/categories.ts` with this exact contents:

    ```typescript
    // Phase 2: static category and model lists for FilterChipBar in SearchPage and CategorySuggestSheet.
    // A DB-driven `categories` table is deferred to a future phase (per 02-RESEARCH.md Open Question 3).
    // Values match the literal labels users see on the chips — they are also stored verbatim in promptys.category.

    export const CATEGORIES = [
      'Retrato',
      'Paisagem',
      'Fantasia',
      'Sci-Fi',
      'Abstrato',
      'Arquitetura',
      'Moda',
      'Comida',
      'Animais',
      'Arte digital',
    ] as const

    export type Category = (typeof CATEGORIES)[number]

    export const MODELS = [
      'Gemini',
      'Midjourney',
      'DALL·E',
      'Stable Diffusion',
      'Flux',
    ] as const

    export type Model = (typeof MODELS)[number]
    ```

    2. Edit `src/App.tsx`. Inside the `ChromeShell` function's `<Routes>` block, add two new `<Route>` entries (with inline placeholder components — plans 02-03 and 02-04 will replace these). The new block must read:

    ```typescript
    <Routes>
      <Route path="/" element={<FeedPage />} />
      <Route path="/saved" element={<SavedPagePlaceholder />} />
      <Route path="/search" element={<SearchPagePlaceholder />} />
      <Route path="/profile" element={<ProfilePage />} />
      <Route path="/u/:username" element={<PublicProfilePage />} />
      <Route path="/p/:slug" element={<PromptyDetailPage />} />
      <Route path="*" element={<Navigate to="/" replace />} />
    </Routes>
    ```

    3. Below the `ChromeShell` function in the same file, add two inline placeholder components. They render a TODO message; plans 02-03 (SearchPage) and 02-04 (SavedPage) will delete these placeholders and add the real page imports.

    ```typescript
    // TODO Plan 02-04: replace with real <SavedPage />
    function SavedPagePlaceholder() {
      return (
        <main className="screen" style={{ padding: 32, textAlign: 'center', maxWidth: 430, margin: '0 auto' }}>
          <p style={{ color: 'var(--text-3)', fontSize: 13.5 }}>
            Salvos em construção — disponível em breve.
          </p>
        </main>
      )
    }

    // TODO Plan 02-03: replace with real <SearchPage />
    function SearchPagePlaceholder() {
      return (
        <main className="screen" style={{ padding: 32, textAlign: 'center', maxWidth: 430, margin: '0 auto' }}>
          <p style={{ color: 'var(--text-3)', fontSize: 13.5 }}>
            Busca em construção — disponível em breve.
          </p>
        </main>
      )
    }
    ```

    DO NOT touch the auth route block (the top-level `<Routes>` with /login, /signup, /reset-password, /onboarding) — only the inner `ChromeShell` routes change.

    4. Run `pnpm test:run` and `pnpm build` after the edits.
  </action>
  <verify>
    <automated>test -f src/lib/constants/categories.ts && grep -q "export const CATEGORIES" src/lib/constants/categories.ts && grep -q "export const MODELS" src/lib/constants/categories.ts && grep -q "Retrato" src/lib/constants/categories.ts && grep -q "Gemini" src/lib/constants/categories.ts && grep -q "path=\"/saved\"" src/App.tsx && grep -q "path=\"/search\"" src/App.tsx && grep -q "SavedPagePlaceholder" src/App.tsx && grep -q "SearchPagePlaceholder" src/App.tsx && pnpm test:run && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/constants/categories.ts` exists and exports `CATEGORIES` and `MODELS` as `as const` tuples
    - `CATEGORIES` contains exactly 10 entries matching UI-SPEC list (Retrato, Paisagem, Fantasia, Sci-Fi, Abstrato, Arquitetura, Moda, Comida, Animais, Arte digital)
    - `MODELS` contains exactly 5 entries (Gemini, Midjourney, DALL·E, Stable Diffusion, Flux)
    - `src/App.tsx` ChromeShell routes contain `<Route path="/saved" element={<SavedPagePlaceholder />} />` and `<Route path="/search" element={<SearchPagePlaceholder />} />`
    - `src/App.tsx` declares functions `SavedPagePlaceholder` and `SearchPagePlaceholder` returning the placeholder JSX described above
    - Full test suite passes: `pnpm test:run`
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>Routes /saved and /search reachable; categories/models constants ready for Plan 02-03 + 02-04 + 02-05 to import.</done>
</task>

</tasks>

<verification>
- TabBar shows Salvos between Feed and Buscar for L2 users — confirmed by RTL test
- L1 users see no Salvos, no Buscar (LEVL-07) — confirmed by RTL test
- Navigating to /saved and /search renders placeholder text (no 404, no Navigate redirect)
- Icon.tsx exports 3 new names that subsequent components can consume
- categories.ts constants ready for FilterChipBar in plans 02-03 and 02-05
</verification>

<success_criteria>
- TabBar.tsx + TabBar.test.tsx updated: L1=2 tabs, L2=4 tabs, L3=6 tabs
- Icon.tsx has 3 new paths
- App.tsx has /saved and /search routes with placeholder pages
- categories.ts module exports CATEGORIES + MODELS constants
- Full test suite + build green
</success_criteria>

<output>
After completion, create `.planning/phases/02-l2-curador-descoberta/02-02-SUMMARY.md` documenting:
- TABS array final order
- Test count delta (existing 5 tests + 1 new L1 test = 6)
- Icon SVG choices (note: moreHorizontal uses filled circles, others stroked — per UI-SPEC engineering note)
- Placeholder pages location in App.tsx and TODO references to plans 02-03/02-04
</output>
