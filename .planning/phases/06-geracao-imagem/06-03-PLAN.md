---
phase: 06-geracao-imagem
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/hooks/useGenerate.ts
  - src/hooks/useGenerate.test.tsx
  - src/pages/PromptyDetailPage.tsx
  - src/pages/PromptyDetailPage.test.tsx
  - CLAUDE.md
  - AGENTS.md
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06, GEN-07]

must_haves:
  truths:
    - "Anonymous user sees the CTA 'Cadastre-se e ganhe 1 crédito para gerar' and clicking it navigates to /signup (GEN-06)"
    - "Logged-in user with credits===0 sees an earn nudge listing real Phase 5 actions (publicar / enviar resultado / subir de nível), not a purchase paywall (GEN-07)"
    - "Logged-in user with credits>=1 sees an enabled 'Gerar imagem (1 crédito)' button beside Copiar; clicking disables it immediately (anti double-click) and shows a ~10s loading state (GEN-03/GEN-05)"
    - "On success the image renders inline and refetchProfile is called so the credit badge decrements; on error the message + 'Seu crédito foi devolvido' shows (GEN-04/GEN-05)"
    - "CLAUDE.md AND AGENTS.md are updated in this same commit with the Edge Function secret exception (GEN-02 spirit)"
  artifacts:
    - path: "src/hooks/useGenerate.ts"
      provides: "generate state machine over supabase.functions.invoke('generate-image')"
      contains: "functions.invoke('generate-image'"
      min_lines: 30
    - path: "src/pages/PromptyDetailPage.tsx"
      provides: "anon CTA + zero-credit nudge + generate button + inline image + error message"
      contains: "Gerar imagem"
    - path: "CLAUDE.md"
      provides: "Edge Function secret exception"
      contains: "Edge Function"
    - path: "AGENTS.md"
      provides: "Edge Function secret exception (kept in sync with CLAUDE.md)"
      contains: "Edge Function"
  key_links:
    - from: "useGenerate.ts"
      to: "generate-image Edge Function"
      via: "supabase.functions.invoke('generate-image', { body: { prompty_id, rendered_prompt } })"
      pattern: "functions\\.invoke\\('generate-image'"
    - from: "useGenerate.ts"
      to: "credit badge"
      via: "useAuthStore.getState().refetchProfile() after invoke resolves"
      pattern: "refetchProfile"
    - from: "PromptyDetailPage.tsx"
      to: "useGenerate + auth store"
      via: "render branches on user / profile.credits / state"
      pattern: "Cadastre-se e ganhe 1 crédito para gerar"
---

<objective>
Wire the frontend: a `useGenerate` hook (state machine over `supabase.functions.invoke('generate-image')`) and the generate button + all states on `PromptyDetailPage`, plus the mandatory CLAUDE.md/AGENTS.md documentation update (Edge Function secret exception). This turns the Wave 0 RED test scaffolds GREEN.

States (LOCKED): anon → signup CTA (GEN-06); logged-in 0 credits → earn nudge, no paywall (GEN-07); logged-in ≥1 → button disables on click + ~10s loading skeleton + inline image + badge decrement (GEN-01/03/05); error → message + "Seu crédito foi devolvido" (GEN-04/05).

Purpose: GEN-01/02/03/04/05/06/07 surfaced in the UI.
Output: hook + page edits + 2 test files filled in + 2 docs files.
</objective>

<execution_context>
@/Users/sierra/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sierra/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-geracao-imagem/06-CONTEXT.md
@.planning/phases/06-geracao-imagem/06-RESEARCH.md
@.planning/phases/05-ganhar-creditos-contribuindo/05-02-PLAN.md
@src/pages/PromptyDetailPage.tsx
@src/hooks/useCopy.ts
@src/stores/auth.store.ts
@src/lib/supabase.ts
@src/components/ui/PrimaryButton.tsx
@src/hooks/useCredits.ts
@CLAUDE.md
@AGENTS.md

<interfaces>
Edge Function response (from plan 06-02): success `{ signed_url, balance }`; failure body `{ error, refunded? }`. `supabase.functions.invoke` returns `{ data, error }`; on non-2xx, `error` is non-null and `error.context?.status` carries the HTTP code (402 = no credits). `data?.error` holds the function's error string. Always check `error` first, then `data?.error`, then `data?.signed_url`.

Earn actions for the GEN-07 nudge (from Phase 5, 05-02-PLAN.md — LOCKED rewards):
- Publicar um prompty: +1 crédito (até 20)
- Enviar um resultado aprovado: +1 crédito (até 10/dia)
- Subir de nível: +2 créditos
Use these to phrase the nudge as encouragement to CONTRIBUTE — never as a purchase prompt.

Existing PromptyDetailPage: action buttons live in the `div` at line ~291 (Copiar prompt PrimaryButton, then Save button for `user`, then Criar variação for L3+). Insert the generate UI in that same container. `user` and `profile` already come from `useAuthStore`; `nav` from `useNavigate`; `resolved` is the rendered prompt string; `prompty.id` is available. PrimaryButton accepts `full`, `disabled`, `onClick`, `icon`, `children` (no inline style prop — for the image/error/nudge use plain elements with inline styles per project convention).

Auth store: `useAuthStore.getState().refetchProfile()` (existing). The existing page mock in PromptyDetailPage.test.tsx is a selector over `{ user, profile }`; if the rendered generate path calls `getState()`, extend the test mock to also return `{ getState: () => ({ refetchProfile: vi.fn() }) }`.

Test scaffolds to fill in (from plan 06-01): `src/hooks/useGenerate.test.tsx` (it.todo for GEN-03/05) and `src/pages/PromptyDetailPage.test.tsx` (it.todo for GEN-06/07). Replace the todos with real assertions; keep the existing tests intact.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useGenerate hook (state machine) + fill its tests GREEN (GEN-03, GEN-05)</name>
  <read_first>
    - src/hooks/useCopy.ts (best-effort + `void useAuthStore.getState().refetchProfile()` pattern — mirror exactly)
    - src/stores/auth.store.ts (refetchProfile signature; getState usage)
    - .planning/phases/06-geracao-imagem/06-RESEARCH.md (Pattern 7 useGenerate lines 246-286; error surface note line 286; invoke shape lines 417-427)
    - src/hooks/useGenerate.test.tsx (the it.todo scaffold from plan 06-01 — fill these in)
    - src/hooks/useCredits.test.ts (existing hook-test mock conventions: vi.mock of @/lib/supabase and @/stores/auth.store)
  </read_first>
  <behavior>
    - Test 1 (GEN-05 success): invoke resolves `{ data: { signed_url: 'https://x/y.webp', balance: 0 }, error: null }` → state transitions idle→loading→done, signedUrl set, errorMsg null
    - Test 2 (GEN-05 error): invoke resolves `{ data: { error: 'no_credits' }, error: {...} }` → state idle→loading→error, errorMsg set, signedUrl null
    - Test 3 (GEN-03 anti double-invoke): calling generate() a second time while a call is in flight does not start a second invoke (synchronous useRef guard, early-return)
    - Test 4 (GEN-01/05 badge): refetchProfile is invoked after the invoke resolves (success AND error)
  </behavior>
  <files>src/hooks/useGenerate.ts, src/hooks/useGenerate.test.tsx</files>
  <action>
**1. Create `src/hooks/useGenerate.ts`** (mirror useCopy's refetchProfile pattern; add the GEN-03 in-flight guard):
```typescript
import { useRef, useState } from 'react'
import { supabase } from '@/lib/supabase'
import { useAuthStore } from '@/stores/auth.store'

export type GenState = 'idle' | 'loading' | 'done' | 'error'

export function useGenerate() {
  const [state, setState] = useState<GenState>('idle')
  const [signedUrl, setSignedUrl] = useState<string | null>(null)
  const [errorMsg, setErrorMsg] = useState<string | null>(null)
  const inFlight = useRef(false)  // GEN-03: synchronous guard against double-invoke

  async function generate(promptyId: string, renderedPrompt: string): Promise<void> {
    if (inFlight.current) return  // second rapid click is a no-op
    inFlight.current = true
    setState('loading')
    setSignedUrl(null)
    setErrorMsg(null)

    const { data, error } = await supabase.functions.invoke('generate-image', {
      body: { prompty_id: promptyId, rendered_prompt: renderedPrompt },
    })

    // Server is the source of truth for credits — refetch on every outcome.
    void useAuthStore.getState().refetchProfile()
    inFlight.current = false

    if (error || !data?.signed_url) {
      setErrorMsg(data?.error ?? error?.message ?? 'Erro ao gerar imagem')
      setState('error')
      return
    }
    setSignedUrl(data.signed_url)
    setState('done')
  }

  return { generate, state, signedUrl, errorMsg, reset: () => setState('idle') }
}
```

**2. Fill `src/hooks/useGenerate.test.tsx`** — remove the anchors/todos from plan 06-01 and add real tests using `renderHook` + `act` (from @testing-library/react). Mock `@/lib/supabase` (functions.invoke) and `@/stores/auth.store` (getState().refetchProfile = a vi.fn()):
- mock `supabase.functions.invoke` per-test to resolve success/error
- assert state transitions and signedUrl/errorMsg
- for GEN-03: make the invoke mock return a manually-controlled (deferred) promise on first call; call `generate()` twice synchronously inside `act`; assert `invoke` mock `toHaveBeenCalledTimes(1)`
- assert `refetchProfile` mock called after the invoke resolves (in both success and error cases)

**3. Run** `pnpm test:run src/hooks/useGenerate.test.tsx` — all tests GREEN (no todos left).
  </action>
  <verify>
    <automated>test -f src/hooks/useGenerate.ts && grep -q "functions.invoke('generate-image'" src/hooks/useGenerate.ts && grep -q "inFlight" src/hooks/useGenerate.ts && grep -q "refetchProfile" src/hooks/useGenerate.ts && ! grep -q "it.todo" src/hooks/useGenerate.test.tsx && pnpm test:run src/hooks/useGenerate.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/useGenerate.ts` exists; `grep -q "functions.invoke('generate-image'"` succeeds
    - GEN-03 guard present: `grep -q "inFlight"` AND an early `return` when already in flight
    - GEN-05 states: hook exposes `state` typed `'idle' | 'loading' | 'done' | 'error'`, plus `signedUrl` and `errorMsg`
    - `grep -q "refetchProfile"` succeeds (badge decrement after invoke)
    - body uses `rendered_prompt` and `prompty_id` keys (matches 06-02 contract): `grep -q "rendered_prompt"` and `grep -q "prompty_id"` succeed
    - `grep -c "it.todo" src/hooks/useGenerate.test.tsx` returns 0 (all todos filled)
    - `pnpm test:run src/hooks/useGenerate.test.tsx` exits 0 with ≥4 passing tests covering success / error / double-invoke / refetch
  </acceptance_criteria>
  <done>useGenerate is a 4-state machine over the Edge Function with a synchronous double-invoke guard and post-invoke profile refetch; its GEN-03/GEN-05 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: PromptyDetailPage generate UI (anon CTA / nudge / button / inline image / error) + fill GEN-06/07 tests GREEN</name>
  <read_first>
    - src/pages/PromptyDetailPage.tsx (action-button container at line ~291; existing Save/Criar variação inline-style button pattern to mirror; `user`, `profile`, `nav`, `resolved`, `prompty.id` in scope)
    - src/components/ui/PrimaryButton.tsx (props: full/disabled/onClick/icon/children; no style prop → use plain elements for image/error/nudge)
    - .planning/phases/06-geracao-imagem/06-RESEARCH.md (Pattern 8 button states lines 288-325)
    - .planning/phases/05-ganhar-creditos-contribuindo/05-02-PLAN.md (earn rewards for the nudge text: publish +1/cap20, approved result +1/cap10-day, level-up +2)
    - src/pages/PromptyDetailPage.test.tsx (the GEN-06/07 it.todo scaffold from plan 06-01 + existing mockUser/mockProfile let-bindings to drive per-test state)
    - CLAUDE.md (Estilo: inline styles are the established pattern; project colors)
  </read_first>
  <files>src/pages/PromptyDetailPage.tsx, src/pages/PromptyDetailPage.test.tsx</files>
  <action>
**1. Edit `src/pages/PromptyDetailPage.tsx`** — import and call `useGenerate` near the other hooks; destructure `{ generate, state, signedUrl, errorMsg }`. Insert the generate UI inside the existing action-button `div` (line ~291), AFTER the Copiar prompt PrimaryButton (alongside the existing `user`/Save block). Use inline styles + project colors (CLAUDE.md). LOCKED states:

- **Anonymous (`!user`):** a button styled like the existing secondary buttons (background var(--surface-2), border 1px solid var(--line)) with EXACT text `Cadastre-se e ganhe 1 crédito para gerar`, `onClick={() => void nav('/signup')}`. (GEN-06)
- **Logged-in, `(profile?.credits ?? 0) === 0`:** a nudge paragraph (NOT a paywall) encouraging contribution, listing the real Phase 5 actions. Exact copy:
  `Sem créditos. Contribua para ganhar mais: publique um prompty (+1), envie um resultado aprovado (+1) ou suba de nível (+2).`
  No "comprar"/"compra"/price ("R$") wording anywhere. (GEN-07)
- **Logged-in, `(profile?.credits ?? 0) >= 1`:** a `PrimaryButton` with `full`, `disabled={state === 'loading'}`, icon `'wand'` (existing Icon name), `onClick={() => void generate(prompty.id, resolved)}`. Label: `state === 'loading' ? 'Gerando imagem (~10s)…' : 'Gerar imagem (1 crédito)'`. The immediate disable on click is the client-side anti-double-click defense (GEN-03). (GEN-01/05)
- **Success (`state === 'done' && signedUrl`):** an inline `<img src={signedUrl} alt="Imagem gerada" style={{ width: '100%', borderRadius: 12, marginTop: 8 }} />`. (GEN-05)
- **Error (`state === 'error'`):** a `<p style={{ color: '#FF3B6B', fontSize: 13, marginTop: 8 }}>` containing `{errorMsg} Seu crédito foi devolvido.` (GEN-04/05)

Keep all existing buttons (Copiar / Salvar / Criar variação), the CommunityResults block, and the eslint-disable header comment. The generate block is additive only.

**2. Fill `src/pages/PromptyDetailPage.test.tsx`** — replace the GEN-06/07 (and GEN-01) `it.todo`s with real assertions using the existing `mockUser`/`mockProfile` let-bindings (set them per-test / in a local `beforeEach`):
- GEN-06: `mockUser = null` → `await screen.findByText(/Cadastre-se e ganhe 1 crédito para gerar/)` present; clicking it asserts `mockNavigate` called with `/signup`
- GEN-07: `mockUser = { id: 'u1' }`, `mockProfile = { id:'u1', points:0, level:'L1', credits:0 }` → nudge text present (matches /Contribua para ganhar mais/i) AND assert NO purchase wording (`expect(screen.queryByText(/comprar|compra|R\$/i)).toBeNull()`)
- GEN-01: `mockProfile.credits = 1` → an enabled button with text /Gerar imagem \(1 crédito\)/ present (assert `.toBeEnabled()` / not disabled)

The existing `mockProfile` type lacks `credits` — widen its type to `{ id: string; points: number; level: string; credits: number } | null`. If the rendered generate path triggers `useAuthStore.getState()`, extend the existing auth-store mock to also expose `getState: () => ({ refetchProfile: vi.fn() })`. Do NOT break existing tests (the mock change must be backward-compatible — existing tests set `mockProfile` without `credits` only when `mockUser` is null, in which case the generate button path is the anon branch; if any existing test sets a logged-in profile without `credits`, add `credits: 0` to it).

**3. Run** `pnpm test:run src/pages/PromptyDetailPage.test.tsx` and `pnpm type-check` — both GREEN; existing tests still pass.
  </action>
  <verify>
    <automated>grep -q "useGenerate" src/pages/PromptyDetailPage.tsx && grep -q "Cadastre-se e ganhe 1 crédito para gerar" src/pages/PromptyDetailPage.tsx && grep -q "Gerar imagem (1 crédito)" src/pages/PromptyDetailPage.tsx && grep -q "Seu crédito foi devolvido" src/pages/PromptyDetailPage.tsx && grep -qi "Contribua para ganhar mais" src/pages/PromptyDetailPage.tsx && pnpm test:run src/pages/PromptyDetailPage.test.tsx && pnpm type-check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useGenerate" src/pages/PromptyDetailPage.tsx` succeeds (hook imported + used)
    - GEN-06: exact string `Cadastre-se e ganhe 1 crédito para gerar` present AND the click handler navigates to `/signup`
    - GEN-07: `grep -qi "Contribua para ganhar mais" src/pages/PromptyDetailPage.tsx` succeeds AND no purchase wording (`grep -ciE "comprar|compra|R\\$" src/pages/PromptyDetailPage.tsx` returns 0)
    - GEN-01: `grep -q "Gerar imagem (1 crédito)" src/pages/PromptyDetailPage.tsx` succeeds; button `disabled={state === 'loading'}`
    - GEN-04/05: `grep -q "Seu crédito foi devolvido" src/pages/PromptyDetailPage.tsx` succeeds; inline `<img` with `alt="Imagem gerada"` present
    - `grep -c "it.todo" src/pages/PromptyDetailPage.test.tsx` returns 0 (todos filled)
    - `pnpm test:run src/pages/PromptyDetailPage.test.tsx` exits 0 (existing tests + new GEN-01/06/07 cases pass)
    - `pnpm type-check` exits 0 (mockProfile.credits widening + page compile clean)
  </acceptance_criteria>
  <done>PromptyDetailPage shows the right surface for every audience — anon CTA, zero-credit earn nudge, the generate button with loading/inline-image/error+refund states — and the GEN-01/06/07 component tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md + AGENTS.md with the Edge Function secret exception (same commit)</name>
  <read_first>
    - CLAUDE.md ("Regras Gerais": currently says "Sem backend próprio no MVP — Supabase é a única infraestrutura"; "Sem window.fetch direto"; the rule-change sync clause "Quando estas regras mudarem, atualize AGENTS.md e CLAUDE.md no mesmo commit")
    - AGENTS.md (Visão Geral line 12 "MVP: Sem backend próprio, sem geração de imagens inline"; "Regras Críticas → Supabase: Nunca use service_role key no frontend"; sync clause line 3)
    - .planning/phases/06-geracao-imagem/06-CONTEXT.md (Documentação decision, lines 50-51 — exact exception wording)
  </read_first>
  <files>CLAUDE.md, AGENTS.md</files>
  <action>
Both files carry a rule-sync clause (update both in the same commit). Add the Edge Function exception to BOTH, with matching meaning.

**1. CLAUDE.md** — in "Regras Gerais", amend/append so the no-backend rule has an explicit Edge Function carve-out. Add a bullet such as:
```
- **Exceção Edge Function (Supabase):** Edge Functions (Deno) são permitidas para guardar segredos de terceiros (ex.: API key de geração de imagem) e orquestrar gasto/refund de crédito server-side. O frontend continua anon-key client-side only e NUNCA recebe segredos. A única Edge Function do projeto é `supabase/functions/generate-image/`. Chaves de provedor vivem apenas em `Deno.env` (via `supabase secrets set`), nunca em `VITE_*` nem no bundle.
```
Update the existing "Sem backend próprio no MVP" line to reference this exception (e.g., append "— exceto Edge Functions seguras para segredos de terceiros, ver abaixo").

**2. AGENTS.md** — mirror the same exception:
- In "Visão Geral", soften the "MVP: Sem backend próprio, sem geração de imagens inline" line — geração de imagem inline now exists via a secure Edge Function.
- In "Regras Críticas → Supabase", add a bullet matching CLAUDE.md: Edge Functions may hold third-party secrets server-side; frontend stays anon-key only; the sole function is `generate-image`; provider keys live in `Deno.env`, never in `VITE_*`.

Keep wording consistent across both files (the sync clause requires it). Do not remove the existing "Nunca use service_role key no frontend" rule — the Edge Function uses the auto-injected service-role key server-side, which is exactly why the frontend never needs it.

**3. Verify the key-leakage invariant still holds** (GEN-02 success criterion 4):
```
grep -rE "VITE_.*(KEY|TOKEN|SECRET)" src/ | grep -v ".test."
```
This MUST return empty (the only VITE_ keys are SUPABASE_URL/ANON_KEY in src/lib/supabase.ts which are not KEY/TOKEN/SECRET-suffixed beyond the anon key — confirm the grep is empty; ANON_KEY is the public anon key and is expected, but the pattern `VITE_.*KEY` would match `VITE_SUPABASE_ANON_KEY`). NOTE: the documented success-criterion grep is `VITE_.*KEY\|VITE_.*TOKEN`; `VITE_SUPABASE_ANON_KEY` is the PUBLIC anon key and is allowed (it is safe to ship). The real invariant is that NO provider/service-role secret is in src/. Confirm `grep -rE "SERVICE_ROLE|GEMINI|OPENAI|REPLICATE" src/` returns empty.
  </action>
  <verify>
    <automated>grep -qi "Edge Function" CLAUDE.md && grep -qi "Deno.env" CLAUDE.md && grep -qi "Edge Function" AGENTS.md && grep -qi "generate-image" CLAUDE.md && [ -z "$(grep -rE "SERVICE_ROLE|GEMINI_API_KEY|OPENAI_API_KEY|REPLICATE_API_TOKEN" src/ 2>/dev/null)" ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md: `grep -qi "Edge Function"` AND `grep -qi "Deno.env"` AND `grep -qi "generate-image"` succeed; the no-backend line references the exception
    - AGENTS.md: `grep -qi "Edge Function"` succeeds AND the Supabase rules mention server-side secrets allowed in Edge Functions, frontend anon-key only
    - Both files still contain the rule-sync clause and the "service_role nunca no frontend" rule (not removed)
    - `grep -rE "SERVICE_ROLE|GEMINI_API_KEY|OPENAI_API_KEY|REPLICATE_API_TOKEN" src/` returns empty (no provider/service secret in the bundle — GEN-02)
    - `grep -rE "VITE_.*(KEY|TOKEN|SECRET)" src/ | grep -v ".test."` returns at most the public `VITE_SUPABASE_ANON_KEY` line in src/lib/supabase.ts (the only allowed public key; no provider secrets)
  </acceptance_criteria>
  <done>CLAUDE.md and AGENTS.md both document the Edge Function secret exception in one commit; the key-leakage invariant (no provider/service-role secret in src/) is confirmed (GEN-02).</done>
</task>

</tasks>

<verification>
- useGenerate hook present; its GEN-03/GEN-05 tests GREEN
- PromptyDetailPage shows anon CTA / zero-credit nudge / generate button / inline image / error+refund; GEN-01/06/07 tests GREEN
- pnpm test:run (full suite) and pnpm type-check both GREEN
- CLAUDE.md + AGENTS.md updated together with the Edge Function exception
- key-leakage grep: no provider/service-role secret in src/ (GEN-02)
</verification>

<success_criteria>
The button on PromptyDetailPage adapts to the viewer: anonymous → signup CTA (GEN-06); zero-credit → contribute nudge, not a paywall (GEN-07); funded → generate with immediate disable + ~10s loading + inline result + badge decrement (GEN-01/03/05); failure → error + "Seu crédito foi devolvido" (GEN-04/05). The provider secret never reaches the frontend and the docs codify the Edge Function exception (GEN-02).
</success_criteria>

<output>
After completion, create `.planning/phases/06-geracao-imagem/06-03-SUMMARY.md`
</output>
</content>
